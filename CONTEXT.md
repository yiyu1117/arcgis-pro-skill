# CONTEXT.md — GeoScene/ArcGIS 数据技能（领域模型）

> 本文件是术语表，不含实现细节。设计决议见 `docs/adr/`。

## 领域

用本机 ArcGIS Pro 的 arcpy（GeoScene/ArcGIS Pro 自带 Python）处理地理数据。

**领域边界**：只处理磁盘上的数据（shp/gdb/栅格的转换、投影、叠加、字段计算、批量处理）。**不包含** ArcGIS Pro 界面控制（缩放/开关图层/符号/制图导出）——那是独立的"GUI 控制"技能，与本技能分离。

## 解释器配置（跨机器）

技能用于**多台机器**。**ArcGIS Pro 与 GeoScene Pro 互斥，一台机器只能安装其中一个**，因此单机至多一个解释器候选。设计：

- **首次运行**：检测 arcpy 解释器路径（注册表 + 常见安装路径，同时覆盖 ArcGIS Pro 与 GeoScene），**写入技能内配置文件**
- **后续运行**：直接读配置使用，不再检测
- **仅当报错找不到解释器**（配置路径失效）时，才重新检测并更新配置
- 不假设固定路径，两台机器各自自适应

## 产物组织（artifact organization）

- 工作目录下分 **过程/**（中间产物）与 **结果/**（最终成果），各含**语义命名的产物子文件夹**
- 每个中间产物在 `过程/` 下占一个子文件夹，每个最终成果在 `结果/` 下占一个
- **命名规范**：处理后状态 + 内容，使用专业命名——如"镶嵌后DEM"、"用于裁剪的矢量"、"裁剪后DEM"；不用模糊口语词（"镶嵌后的"→"镶嵌后DEM"）
- 过程与结果严格分开，不混放
- **原始数据留在原位，只读，绝不修改**——不强制挪进 input/ 文件夹
- `_运行记录/` 保留两个文件：**audit.md**（轻量运行记录，可不逐条严谨记录）与 **decisions.md**（报错采用的修复方式，跨会话复用——这是重点，保留完整）

## 核心概念

- **执行单元（execution unit / step）**：整个技能的基本操作单位——一次独立执行、能独立判定成败的 arcpy 动作。一个任务 = 一串执行单元的序列。
  动机：长脚本一次跑完，出错时只看到 ERROR 999999 / 退出码 -1，无法定位到具体命令。

- **执行方式（execution style）**：默认**内联执行**（`python.exe -c "..."`），不留脚本文件；**常见错误的处理方式提前预存在技能内**，报错即查即修。多行复杂逻辑（循环/游标等）时例外，才写脚本文件运行。

- **To Do 任务列表**：To Do 项用 Claude Code **内置任务工具**（TaskCreate/TaskUpdate）管理——拆解时建任务，完成一项更新一项，界面有实体对应。等待后台/长任务用内置等待/通知机制，禁止 sleep 轮询。

- **步骤声明（step declaration）**：调用任何对数据有影响的操作之前，必须以正文输出人话版说明【操作名 + 输入 + 关键参数】供用户核对，**不需要等待确认，显示即执行**。这是硬性动作，不是建议。

- **任务拆解（task decomposition）**：复杂任务先被拆成 To Do 项（阶段）清单（计划），每项内部再含若干执行单元。拆解粒度由模型提出，用户在计划确认处可调整（如"镶嵌+裁剪合并成一项"）。

- **阶段确认点（checkpoint）**：每个 To Do 项执行完必须停下，报告该阶段成果，等用户确认后才进入下一项。确认点是强制暂停，不是可选项。

- **计划确认（plan confirmation）**：To Do 清单拆解完成后、执行第一个 To Do 项之前，需先整体确认计划。

- **快进（fast-forward）**：用户确认计划后，可指示"某几步连续执行"，把中间执行单元合并为一次进程执行。动机：每次 `import arcpy` 开销约 5~10 秒，连续小步会拖慢速度。

- **可复现（reproducibility）**：相同输入 + 相同步骤 → 相同输出。步骤一旦确定不允许重写，每步输出落盘到固定路径。

- **错误定位（error localization）**：任何失败必须能归因到具体执行单元。报错不得以"无原因"收场。

- **原生崩溃（native crash）**：arcpy 原生层崩溃——进程退出码 `-1`，无 Python traceback，仅显示 Pro 的致命错误对话框。判定为原生崩溃后进入修复循环，重点是**逐项核对参数与工具官方签名**（崩溃通常由参数错误引起）。弹窗"未保存数据将丢失"是误导文案，实际不会丢失，不需为此提醒用户。

- **超时保护（timeout）**：执行单元使用 Bash 自带超时——默认 10 分钟，数据量大时 20~30 分钟。超时后**转后台任务继续运行**，并用后台任务等待/通知机制等待结果，**禁止用 sleep 轮询**。

- **卡死根因 = 数据量过大**：工具长时间无输出通常是因为数据量爆炸，多因**处理顺序不当**——本应先裁剪/选择缩小范围再计算，模型却先全量计算再裁剪。拆解阶段必须评估"能否先缩小数据再处理"。

- **参数确凿（parameter certainty）**：常用 arcpy 工具（约 20 个）的签名**内联在 SKILL.md 内**，模型每次加载技能即看到，零额外读取成本；罕见工具联网查询官方文档（`site:pro.arcgis.com`）。不使用大量独立参考文件。

- **修复循环（repair loop）**：单步执行定位到出错执行单元后，允许联网查询（arcpy 官方文档等）并尝试修复，**最多 3 次**；每次尝试必须基于新信息，禁止重复同一修法。3 次仍失败 → 中断整个操作并报告。

- **修复伦理（repair ethics）**：修复必须根治问题，**禁止用简易方式替代**——不得更换工具语义、跳过失败数据、用近似代替精确、降低数据范围/精度来"让任务通过"。修复结果必须与用户确认的计划语义一致。

## 用户（操作者）

- 用中文与技能交互。
- 允许技能严谨/流程复杂，但整体目标是**提高处理地理数据的速度**。
- 需要在步骤声明处有核对窗口，但不耐烦逐条确认。
